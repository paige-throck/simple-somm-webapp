(function() {
  'use strict'

  angular.module('app')
    .component('profile-ocr', {
      controller: controller,
      template: `

        
      `
    })

  controller.$inject = ['$http']

  function controller($http) {

  }
}());
