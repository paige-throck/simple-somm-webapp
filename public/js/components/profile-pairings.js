(function() {
  'use strict'

  angular.module('app')
    .component('profile-pairings', {
      controller: controller,
      template: `

      `
    })

  controller.$inject = ['$http']

  function controller($http) {

  }
}());
